import { LineChartDirective } from './line-chart.directive';

describe('LineChartDirective', () => {
  it('should create an instance', () => {
    const directive = new LineChartDirective(null as any);
    expect(directive).toBeTruthy();
  });
});
